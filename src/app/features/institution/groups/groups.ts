import {Component,inject,signal,OnInit} from '@angular/core';import {InstitutionPortalService} from '../../../core/services/institution-portal.service';import {InstitutionGroup} from '../../../core/models/institution-portal.model';@Component({selector:'app-groups',standalone:true,templateUrl:'./groups.html',styleUrl:'./groups.css'})export class InstitutionGroups implements OnInit{private s=inject(InstitutionPortalService);items=signal<InstitutionGroup[]>([]);ngOnInit(){this.load()}load(){this.s.groups().subscribe(r=>this.items.set(r.data??[]))}}
