import {Component,inject,signal,OnInit} from '@angular/core';import {InstitutionPortalService} from '../../../core/services/institution-portal.service';import {VoltsDevice} from '../../../core/models/institution-portal.model';@Component({selector:'app-devices',standalone:true,templateUrl:'./devices.html',styleUrl:'./devices.css'})export class InstitutionDevices implements OnInit{private s=inject(InstitutionPortalService);items=signal<VoltsDevice[]>([]);ngOnInit(){this.load()}load(){this.s.devices().subscribe(r=>this.items.set(r.data??[]))}}
