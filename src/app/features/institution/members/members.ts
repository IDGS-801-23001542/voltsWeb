import {Component,inject,signal,OnInit} from '@angular/core';import {InstitutionPortalService} from '../../../core/services/institution-portal.service';import {InstitutionMember} from '../../../core/models/institution-portal.model';@Component({selector:'app-members',standalone:true,templateUrl:'./members.html',styleUrl:'./members.css'})export class InstitutionMembers implements OnInit{private s=inject(InstitutionPortalService);items=signal<InstitutionMember[]>([]);ngOnInit(){this.load()}load(){this.s.members().subscribe(r=>this.items.set(r.data??[]))}}
