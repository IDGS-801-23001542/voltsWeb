import {Component,inject,signal,OnInit} from '@angular/core';import {InstitutionPortalService} from '../../../core/services/institution-portal.service';import {License} from '../../../core/models/license.model';@Component({selector:'app-licenses',standalone:true,templateUrl:'./licenses.html',styleUrl:'./licenses.css'})export class InstitutionLicenses implements OnInit{private s=inject(InstitutionPortalService);items=signal<License[]>([]);ngOnInit(){this.load()}load(){this.s.licenses().subscribe(r=>this.items.set(r.data??[]))}}
